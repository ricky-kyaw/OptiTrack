<Window x:Class="OptiTrack.App.UI.employees.EmployeeDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrls="clr-namespace:OptiTrack.App.UI.ctrls"
        mc:Ignorable="d"
        Title="Employee Dashboard" Height="600" Width="900"
        Background="#F5F5F5">

    <Window.Resources>

        <!-- 🔶 Overtime Button Style -->
        <Style x:Key="OvertimeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF6B35"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E55A2B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 🟩 Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0"
                                      Direction="270"
                                      ShadowDepth="2"
                                      BlurRadius="8"
                                      Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>

        <!-- ✅ HEADER -->
        <Border Background="White"
                Height="70"
                VerticalAlignment="Top"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">

            <Grid Margin="30,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="40" Height="40" Fill="#4A90E2" Margin="0,0,12,0">
                        <Ellipse.Effect>
                            <DropShadowEffect Color="#4A90E2" BlurRadius="10" Opacity="0.3"/>
                        </Ellipse.Effect>
                    </Ellipse>

                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Employee Portal"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#333333"/>
                        <TextBlock Text="{Binding EmployeeName}"
                                   FontSize="12"
                                   Foreground="#888888"/>
                    </StackPanel>
                </StackPanel>

                <Button Content="Logout"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        Foreground="#666666"
                        BorderThickness="1"
                        BorderBrush="#CCCCCC"
                        Padding="16,8"
                        FontSize="13"
                        Cursor="Hand"
                        Click="LogoutButton_Click"/>
            </Grid>
        </Border>

        <!-- ✅ MAIN CONTENT -->
        <ScrollViewer Margin="0,70,0,0" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="30" MaxWidth="800">

                <!-- ✅ EMPLOYEE INFORMATION CARD -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Your Information"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#333333"
                                   Margin="0,0,0,20"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <!-- ✅ EMPLOYEE NAME -->
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Employee Name"
                                           FontSize="12"
                                           Foreground="#888888"/>
                                <TextBlock Text="{Binding EmployeeName}"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           Foreground="#333333"/>
                            </StackPanel>

                            <!-- ✅ DEPARTMENT -->
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Department"
                                           FontSize="12"
                                           Foreground="#888888"/>
                                <TextBlock Text="{Binding Department}"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           Foreground="#333333"/>
                            </StackPanel>

                            <!-- ✅ ROLE -->
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Role"
                                           FontSize="12"
                                           Foreground="#888888"/>
                                <TextBlock Text="{Binding UserRole}"
                                           FontSize="14"
                                           FontWeight="Medium"
                                           Foreground="#333333"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- ✅ CLOCK IN / CLOCK OUT SIDE BY SIDE -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource CardStyle}" Grid.Column="0" Margin="0,0,10,0">
                        <ctrls:ClockIn EmployeeId="{Binding EmployeeId}" />
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Grid.Column="1" Margin="10,0,0,0">
                        <ctrls:ClockOut EmployeeId="{Binding EmployeeId}" />
                    </Border>
                </Grid>

                <!-- ✅ OVERTIME CARD -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="My Overtime"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="#333333"
                                   Margin="0,0,0,8"/>

                        <TextBlock Text="View overtime history and earnings"
                                   FontSize="13"
                                   Foreground="#888888"
                                   Margin="0,0,0,16"/>

                        <Button Content="View Overtime"
                                Style="{StaticResource OvertimeButtonStyle}"
                                HorizontalAlignment="Stretch"
                                Click="ViewOvertimeButton_Click"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>
