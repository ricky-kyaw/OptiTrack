<UserControl x:Class="OptiTrack.App.UI.ctrls.ClockOut"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <UserControl.Resources>

        <Style x:Key="ClockOutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C82333"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Border Background="White" CornerRadius="8" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Center" Grid.Row="0">
                <Ellipse Width="50" Height="50" Fill="#F8D7DA" Margin="0,0,0,12">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#DC3545" Opacity="0.25" BlurRadius="15"/>
                    </Ellipse.Effect>
                </Ellipse>

                <TextBlock Text="Clock Out"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#333333"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="End your work shift"
                           FontSize="13"
                           Foreground="#888888"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Row="1"
                    Content="Clock Out"
                    Style="{StaticResource ClockOutButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Click="ClockOutButton_Click"/>
        </Grid>
    </Border>
</UserControl>
