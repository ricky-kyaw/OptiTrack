<UserControl x:Class="OptiTrack.App.UI.ctrls.ClockIn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <UserControl.Resources>

        <Style x:Key="ClockInButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#218838"/>
                </Trigger>
            </Style.Triggers>
        </Style>

    </UserControl.Resources>

    <Border Background="White" CornerRadius="8" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <!-- Content vertically stretches here -->
                <RowDefinition Height="*" />
                <!-- Button sits at bottom -->
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Top content -->
            <StackPanel HorizontalAlignment="Center" Grid.Row="0">
                <Ellipse Width="50" Height="50" Fill="#D4EDDA" Margin="0,0,0,12">
                    <Ellipse.Effect>
                        <DropShadowEffect Color="#28A745" Opacity="0.25" BlurRadius="15"/>
                    </Ellipse.Effect>
                </Ellipse>

                <TextBlock Text="Clock In"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Foreground="#333333"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Start your work shift"
                           FontSize="13"
                           Foreground="#888888"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Button -->
            <Button Grid.Row="1"
                    Content="Clock In"
                    Style="{StaticResource ClockInButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Click="ClockInButton_Click"/>
        </Grid>
    </Border>
</UserControl>
